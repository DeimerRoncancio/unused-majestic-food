// Importar la fuente Inter de Google Fonts
import { Inter } from 'next/font/google'

// Importar el proveedor de contexto OrdersProvider
import { OrdersProvider } from '@/context/OrdersContext'

// Importar componentes y estilos necesarios
import NavBar from '@/components/navbar'
import Providers from './Provider'
import Space from '@/components/space'
import './globals.css'

// Configurar la fuente Inter
const inter = Inter({
    subsets: ['latin']
})

// Metadatos de la aplicación
export const metadata = {
    title: 'Majestic Food',
    description: 'Generated by create next app',
}

// Componente principal para el diseño raíz de la aplicación
export default function RootLayout({ children }) {
    return (
        <html lang="es" className='font-mono'>
            <body className={inter.className}>
                <main className="flex min-h-screen flex-col relative">
                    {/* Proveedores de contexto para manejar pedidos y otros proveedores */}
                    <OrdersProvider>
                        <Providers>
                            {/* Barra de navegación principal */}
                            <NavBar />

                            {/* Espacio adicional para el diseño */}
                            <Space />

                            {/* Contenido principal de la aplicación */}
                            {children}
                        </Providers>
                    </OrdersProvider>
                </main>
            </body>
        </html>
    )
}
